interface:
  display_name: "AVE Data REST"
  short_description: "Search tokens, prices, klines, holders, risk reports via AVE Cloud REST API"
  default_prompt: "Use $ave-data-rest to query on-chain token data from the AVE Cloud Data API."

policy:
  allow_implicit_invocation: true
