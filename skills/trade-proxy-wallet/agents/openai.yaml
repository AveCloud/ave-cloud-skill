interface:
  display_name: "AVE Proxy Wallet Trading"
  short_description: "Server-managed wallet trading with market/limit orders, TP/SL on AVE Cloud"
  default_prompt: "Use $ave-trade-proxy-wallet to manage proxy wallets and place market or limit orders via AVE Cloud."

policy:
  allow_implicit_invocation: false
